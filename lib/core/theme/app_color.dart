import 'package:flutter/material.dart';

/// App color palette
/// Contains all colors used throughout the application
class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  // Primary Colors
  static const Color primaryColor = Color(0xFF9775FA);
  static const Color secondaryColor = Color(0xFFFF7043);

  // Dark Theme Colors
  static const Color darkBackground = Color(0xFF1D1E20);
  static const Color darkSurface = Color(0xFF8F959E);
  static const Color darkCard = Color(0xFFF5F6FA);

  // Light Theme Colors
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightSurface = Color(0xFFF5F8FB);

  // Neutral/Gray Colors
  static const Color darkNavy = Color(0xFF1B262C);
  static const Color mediumGray = Color(0xFF222E34);
  static const Color slateGray = Color(0xFF29363D);
}
